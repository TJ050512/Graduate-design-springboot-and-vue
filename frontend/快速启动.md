# Vue前端快速启动指南

## 📦 第一步：安装依赖

```bash
cd frontend
npm install
```

如果npm安装慢，可以使用cnpm或yarn：

```bash
# 使用cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install

# 或使用yarn
npm install -g yarn
yarn install
```

## 🚀 第二步：启动开发服务器

```bash
npm run dev
```

启动成功后，访问：**http://localhost:3000**

## ✅ 第三步：验证

1. 打开浏览器访问 http://localhost:3000
2. 使用默认账号登录：
   - 用户名：`admin`
   - 密码：`admin123`
3. 登录成功后进入系统首页

## 🔧 前置要求

- ✅ Node.js 16+ 已安装
- ✅ 后端服务已启动（http://localhost:8080）
- ✅ MySQL 和 Redis 已启动

## 📝 常用命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

## ❌ 常见问题

### 问题1：npm install 失败

**解决方法**：
```bash
# 清除npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题2：端口3000被占用

**解决方法**：
```bash
# 查找占用3000端口的进程
lsof -i :3000

# 杀死进程
kill -9 <进程ID>
```

或修改 `vite.config.js` 中的端口号。

### 问题3：无法连接后端API

**检查**：
1. 后端服务是否已启动（http://localhost:8080）
2. 检查浏览器控制台的网络请求
3. 确认 `vite.config.js` 中的代理配置正确

### 问题4：登录后跳转失败

**检查**：
1. 后端 `/api/user/info` 接口是否正常
2. 检查浏览器控制台的错误信息
3. 确认Token是否正确存储

## 🎯 下一步

- 完善业务页面功能
- 添加数据统计图表
- 优化用户体验
- 添加权限控制

