# 🚀 快速启动指南

## 第一步：启动前置服务

### 1. 启动 MySQL

```bash
# 检查MySQL是否运行
brew services list | grep mysql

# 如果未运行，启动MySQL
brew services start mysql

# 验证MySQL是否启动成功
mysql -u root -e "SELECT 1"
```

### 2. 启动 Redis

```bash
# 检查Redis是否运行
brew services list | grep redis

# 如果未运行，启动Redis
brew services start redis

# 验证Redis是否启动成功
redis-cli ping
# 应该返回: PONG
```

### 3. 创建数据库（如果还没有）

```bash
# 创建数据库
mysql -u root -e "CREATE DATABASE IF NOT EXISTS water_management DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 导入初始化数据（如果还没有）
mysql -u root water_management < sql/init.sql
```

---

## 第二步：启动Spring Boot项目

### 方式1：在IDE中运行（推荐）

1. 打开文件：`src/main/java/com/waterworks/WaterManagementApplication.java`
2. 在 `main` 方法上方点击 **▶️ Run** 按钮
3. 或者右键点击文件 → 选择 `Run 'WaterManagementApplication'`

### 方式2：使用终端运行

```bash
# 进入项目目录
cd /Users/maxiaoxiao/Documents/GitHub/graduate-design-springboot-and-vue

# 运行项目（使用IDE内置的Maven）
# 或者如果你安装了Maven：
mvn spring-boot:run
```

---

## 第三步：验证启动

启动成功后，你应该看到：

```
========================================
水务管理系统启动成功！
接口文档地址: http://localhost:8080/api/swagger-ui.html
API文档JSON: http://localhost:8080/api/api-docs
========================================
```

### 访问API文档

打开浏览器访问：
- **Swagger UI**: http://localhost:8080/api/swagger-ui.html
- **API Docs JSON**: http://localhost:8080/api/api-docs

---

## ❌ 常见启动错误

### 错误1：端口8080被占用

```bash
# 查找占用8080端口的进程
lsof -i :8080

# 杀死进程
kill -9 <进程ID>
```

### 错误2：MySQL连接失败

**错误信息**：`Communications link failure` 或 `Access denied`

**解决方法**：
1. 检查MySQL是否启动：`brew services list`
2. 检查配置文件中的密码是否正确（`application.yml` 第17行）
3. 检查数据库是否存在：`mysql -u root -e "SHOW DATABASES;"`

### 错误3：Redis连接失败

**错误信息**：`Unable to connect to Redis`

**解决方法**：
1. 启动Redis：`brew services start redis`
2. 测试连接：`redis-cli ping`

### 错误4：依赖下载失败

**解决方法**：
1. 在IDE中：`Cmd + Shift + P` → `Java: Clean Java Language Server Workspace`
2. 等待依赖重新下载

---

## 🔍 检查项目是否正在运行

```bash
# 检查8080端口是否被占用
lsof -i :8080

# 如果看到Java进程，说明项目正在运行
```

---

## 📝 测试接口

启动成功后，在Swagger UI中测试登录接口：

**POST** `/api/user/login`

**请求体**：
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应**：会返回token，用于后续接口调用。

---

**如果遇到其他问题，请查看IDE的控制台输出，那里会有详细的错误信息！**


