# 🚀 项目运行指南

## 前置要求

✅ Java JDK 已安装：`/opt/homebrew/opt/openjdk`  
✅ Maven 已配置（Spring Boot内置）  
⚠️ 需要安装 MySQL 和 Redis

---

## 📦 第一步：安装必需的扩展

在Cursor中按 `Cmd + Shift + X` 打开扩展商店，安装以下扩展：

### 必装：
1. **Extension Pack for Java** (by Microsoft)
2. **Spring Boot Extension Pack** (by VMware)

安装完成后，重启Cursor IDE。

---

## 🗄️ 第二步：配置数据库

### 1. 安装并启动 MySQL

```bash
# 安装MySQL
brew install mysql

# 启动MySQL服务
brew services start mysql

# 登录MySQL（默认无密码）
mysql -u root
```

### 2. 创建数据库并初始化

在MySQL中执行：

```sql
-- 创建数据库
CREATE DATABASE water_management DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 退出MySQL
exit;
```

然后在终端执行SQL脚本：

```bash
# 进入项目目录
cd /Users/maxiaoxiao/Documents/GitHub/graduate-design-springboot-and-vue

# 执行初始化脚本
mysql -u root water_management < sql/init.sql

# 如果设置了密码，使用：
mysql -u root -p water_management < sql/init.sql
```

### 3. 安装并启动 Redis

```bash
# 安装Redis
brew install redis

# 启动Redis服务
brew services start redis

# 测试Redis是否运行
redis-cli ping
# 如果返回 PONG 说明运行成功
```

---

## ⚙️ 第三步：修改配置文件

编辑 `src/main/resources/application-dev.yml`，修改数据库密码（如果有）：

```yaml
spring:
  datasource:
    druid:
      username: root
      password: 你的MySQL密码  # 如果没有密码就保持123456或留空
```

---

## 🎯 第四步：运行项目

### 方式1：使用Maven命令（推荐）

在终端中执行：

```bash
# 进入项目目录
cd /Users/maxiaoxiao/Documents/GitHub/graduate-design-springboot-and-vue

# 清理并编译项目
mvn clean install -DskipTests

# 运行项目
mvn spring-boot:run
```

### 方式2：使用IDE的运行按钮

1. 打开文件：`src/main/java/com/waterworks/WaterManagementApplication.java`
2. 在main方法上方会出现 `▶️ Run` 按钮
3. 点击运行即可

### 方式3：使用调试配置

1. 按 `Cmd + Shift + D` 打开调试面板
2. 选择"启动水务管理系统"配置
3. 点击绿色运行按钮

---

## 🎉 第五步：验证运行

启动成功后，你会看到：

```
========================================
水务管理系统启动成功！
接口文档地址: http://localhost:8080/api/doc.html
========================================
```

### 访问以下地址：

- **API文档**: http://localhost:8080/api/doc.html
- **Druid监控**: http://localhost:8080/api/druid/login.html
  - 用户名：admin
  - 密码：admin

---

## 🧪 测试接口

### 使用默认账号登录

在API文档中找到"用户管理"，使用以下账号测试登录接口：

```json
{
  "username": "admin",
  "password": "admin123"
}
```

登录成功后会返回token，复制token后在页面右上角"Authorize"按钮处添加：

```
Bearer 你的token
```

---

## ❌ 常见问题

### 问题1：端口被占用

```bash
# 查找占用8080端口的进程
lsof -i :8080

# 杀死进程
kill -9 进程ID
```

### 问题2：MySQL连接失败

- 检查MySQL服务是否启动：`brew services list`
- 检查用户名密码是否正确
- 检查数据库是否已创建

### 问题3：Redis连接失败

- 检查Redis服务是否启动：`brew services list`
- 启动Redis：`brew services start redis`

### 问题4：Maven依赖下载失败

```bash
# 清理Maven缓存
mvn clean

# 强制更新依赖
mvn clean install -U
```

---

## 🛠️ 环境变量配置（可选）

在 `~/.zshrc` 中添加：

```bash
# Java
export JAVA_HOME=/opt/homebrew/Cellar/openjdk/25.0.1/libexec/openjdk.jdk/Contents/Home
export PATH=$JAVA_HOME/bin:$PATH

# Maven（如果单独安装）
export MAVEN_HOME=/opt/homebrew/opt/maven
export PATH=$MAVEN_HOME/bin:$PATH
```

然后执行：`source ~/.zshrc`

---

## 📚 下一步

项目运行成功后，你可以：

1. 📖 查看API文档，了解所有接口
2. 🧪 使用Postman或API文档测试接口
3. 💻 开始开发前端Vue项目
4. 📊 查看Druid监控面板了解SQL执行情况

---

**祝你毕业设计顺利！** 🎓







